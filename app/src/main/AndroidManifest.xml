<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.a21240085.popoiuc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/popo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InserirDadosActivity"></activity>
        <activity android:name=".DadosSemOptimizacaoActivity"></activity>
        <activity android:name=".CalculaIUCActivity"></activity>
    </application>

</manifest>